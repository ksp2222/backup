부모가
선언된거

자식에서
따와서 쓸수있고


생성자 게터세터 만드는이유

toString은 Object에 기본 제공되는 메소드이다
모든 클래스는 Object를 상속받기 때문에
따로 toString을 오버라이딩 해야한다

자식클래스에서 기능을 새로 만들기 때문에
부모클래스에선 껍데기(정의만한다)만 있게 된다 <- 추상화
부모 -> 티비켜라 <-안쓰임
자식 -> lg티비켜라 <-실제로 쓰게될것

추상클래스는 인스턴스(부품만들기)화 불가능 (기능을 구현안하고 선언만하는것)
구현부가 없기 때문에 {}작성안함
추상클래스를 상속받은 자식클래스에서 기능을 구현해야한다.

	public abstract void modify();
	public abstract void delete();

실행자 만드는법
메인 메소드 추가
Tv tv = new LgTv();   --> 임포트
tv. 명령어 부르기

tv = new SamsungTv();  --> 임포트
tv. 명령어 부르기

상속문 구분
부모
자식1, 자식2
있다치자
부모엔 두속성(자식1,2)의 공통을 사용하고
자식엔 별개(독립)의 속성을 사용해서 더 편리하게 관리하기 위함